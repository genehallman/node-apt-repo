{
  "name": "apt-repo",
  "description": "An apt repo that uses S3 as a backend and has basic token security",
  "version": "0.0.1",
  "author": "Gene Hallman <gene@livefyre.com>",
  
  "repository": {
    "type": "git",
    "url": "git://github.com/genehallman/node-apt-repo.git"
  },
  "scripts": {
    "start": "node ./lib/apt-repo",
    "test": "./node_modules/.bin/mocha --ui exports tests/*.test.js --reporter spec"
  },
  "dependencies": {
    "tar": ">=0.1.14",
    "knox": ">=0.4.5",
    "express": "*",
    "async": "*",
    "s3": "*",
 	"gpg": "*"
  },
  "engines": {
    "node": ">=0.6.0"
  },
  "devDependencies": {
    "mocha": "*",
    "should": "*"
  }
}
